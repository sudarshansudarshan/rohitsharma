io               # Standard library (no need to include)
re               # Standard library (no need to include)
random           # Standard library (no need to include)
smtplib          # Standard library (no need to include)
time             # Standard library (no need to include)
json             # Standard library (no need to include)
bcrypt==4.0.1
gspread==5.7.1
requests==2.31.0
streamlit==1.26.0
plotly==5.17.0
pandas==2.1.1
google-auth==2.23.4
google-auth-oauthlib==1.0.0
google-api-python-client==2.94.0
oauth2client==4.1.3
Pillow==10.0.1
pytesseract==0.3.10
